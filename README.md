**commands for API**

```js
npm init -y

touch README.md server.js .gitignore

mkdir models routes utils 

npm install --save express@next morgan cors nodemon

npm install --save bcrypt colors dotenv pg

git init

touch utils/erros.js

touch models/user.js routes/auth.js

touch wedding-registration-schema.sql wedding-registration.sql

psql -f wedding-registration.sql

touch .env .env.template

touch config.js

touch db.js
```